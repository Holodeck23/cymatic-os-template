# Knowledge Vault

This folder is where Cymatic OS writes user-specific onboarding output.

The generated `*.md` files in this folder are intentionally ignored by git because they contain personal context, goals, project details, and preferences.

Template-safe scaffold files live in `templates/`. Use `/onboarding start` to create populated vault files, or copy a template into this folder manually if you want to edit by hand.

